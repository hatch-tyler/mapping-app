# Database
DB_USER=gis_user
DB_PASSWORD=gis_password
DB_NAME=gis_db

# Backend
SECRET_KEY=your-256-bit-secret-key-change-in-production
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Frontend
VITE_API_URL=http://localhost:8000

# File storage
UPLOAD_MAX_SIZE_MB=500

# Initial Admin Setup (required for first run)
# Set these to create the initial admin user
INITIAL_ADMIN_EMAIL=admin@yourcompany.com
INITIAL_ADMIN_PASSWORD=your-secure-password
INITIAL_ADMIN_FULL_NAME=System Administrator
EMAIL_CONFIRMATION_TOKEN_EXPIRE_HOURS=24

# Email Settings (optional - if not set, confirmation URLs print to console)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@example.com
SMTP_FROM_NAME=GIS Application
APP_URL=http://localhost:5173
